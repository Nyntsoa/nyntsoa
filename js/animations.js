document.addEventListener('DOMContentLoaded',()=>{const items=document.querySelectorAll('.reveal');if(!('IntersectionObserver'in window)){items.forEach(x=>x.classList.add('visible'));return;}const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target);}}),{threshold:.13});items.forEach(x=>observer.observe(x));const counters=document.querySelectorAll('.counter');const counterObs=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=Number(el.dataset.target||0);let n=0;const step=Math.max(1,Math.ceil(target/55));const tick=()=>{n=Math.min(target,n+step);el.textContent=n+(target===96?'%':target===180?'+':'');if(n<target)requestAnimationFrame(tick)};tick();counterObs.unobserve(el)}),{threshold:.5});counters.forEach(c=>counterObs.observe(c));});